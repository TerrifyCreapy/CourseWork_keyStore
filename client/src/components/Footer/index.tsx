import React from 'react';
import s from "./Footer.module.scss";

const Footer = () => {
    return (
        <div className={s.footer}>
            Footer
        </div>
    );
};

export default Footer;